function [x_train, y_train, x_test, y_test] = partition_dataset(X, Y)
    % split data in a way that training set has all classes samples
    %NOTE - train func of NN already divides X but randomly...
end